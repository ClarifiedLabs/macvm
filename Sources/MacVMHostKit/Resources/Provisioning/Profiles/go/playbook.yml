---
- name: Install Go development tools
  hosts: macvm
  gather_facts: false
  tasks:
    - name: Install Go toolchain and utilities
      ansible.builtin.raw: |
        eval "$(/opt/homebrew/bin/brew shellenv)"
        brew install go gopls golangci-lint delve
      changed_when: false
